<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.jbundle.base.message</groupId>
    <artifactId>jbundle-base-message-reactor</artifactId>
    <version>0.7.8-SNAPSHOT</version>
  </parent>

  <!-- This project is temporary until the circular dependencies have been resolved -->
  <artifactId>org.jbundle.base.message.misc</artifactId>
  <name>jbundle-base-message-misc</name>
  <packaging>bundle</packaging>

  <build>
    <plugins>
      <plugin>
        <inherited>true</inherited>
        <groupId>org.apache.felix</groupId>
        <artifactId>maven-bundle-plugin</artifactId>
        <configuration>
          <instructions>
            <Export-Package>
            org.jbundle.base.message.app.*;version=${project.version},
            org.jbundle.base.message.opt.*;version=${project.version},
            org.jbundle.base.message.record.*;version=${project.version},
            org.jbundle.base.message.remote.*;version=${project.version},
            org.jbundle.base.message.trx.*;version=${project.version},
            </Export-Package>
            <Import-Package>
            	*;resolution:=optional,
            </Import-Package>
          </instructions>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile> 
      <id>sonatype-oss-release</id>
      <build>
      <!-- ***CAREFUL*** This logic moves the source code to the current project, then
      does NOT compile it. This way, the api and source are in the repo. -->
	    <plugins>
	      <plugin>
	        <artifactId>maven-resources-plugin</artifactId>
	        <executions>
          <execution>
            <id>copy-jibx-bind</id>
            <phase>validate</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${basedir}/src/main/java</outputDirectory>
              <resources>          
                <resource>
                  <directory>${baseMixedDir}/src/main/java</directory>
                  <filtering>false</filtering>
                  <includes>
                    <include>org/jbundle/base/message/app/**/*.java</include>
                    <include>org/jbundle/base/message/opt/**/*.java</include>
                    <include>org/jbundle/base/message/record/**/*.java</include>
                    <include>org/jbundle/base/message/remote/**/*.java</include>
                    <include>org/jbundle/base/message/trx/**/*.java</include>
                  </includes>
                </resource>
              </resources>              
            </configuration>            
          </execution>
	        </executions>
	      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>**</exclude>
          </excludes>
        </configuration>
      </plugin>
	    </plugins>
      </build>
    </profile>
  </profiles>
  
  <dependencies>
    <dependency>
      <groupId>org.jbundle.base.message</groupId>
      <artifactId>org.jbundle.base.message.core</artifactId>
      <version>0.7.8-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>org.jbundle.base</groupId>
      <artifactId>org.jbundle.base.mixed</artifactId>
      <version>0.7.8-SNAPSHOT</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

</project>
